"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.logToDb = logToDb;
const database_1 = require("@genai/database");
async function logToDb(db, params) {
    await db.insert(database_1.actionLogs).values({
        runId: params.runId,
        stepIndex: params.stepIndex,
        actionType: params.actionType,
        thought: params.thought,
        parameters: params.parameters || null,
        screenshotUrl: params.screenshotUrl || null,
    });
}
//# sourceMappingURL=logToDb.js.map