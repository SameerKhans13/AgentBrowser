import { Page } from 'playwright';
export interface InteractiveElement {
    id: number;
    tagName: string;
    role: string;
    label: string;
    x: number;
    y: number;
}
export declare function detectInteractiveElements(page: Page): Promise<InteractiveElement[]>;
