"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const express_1 = __importDefault(require("express"));
const cors_1 = __importDefault(require("cors"));
const database_1 = require("@genai/database");
const drizzle_orm_1 = require("drizzle-orm");
const crypto_1 = __importDefault(require("crypto"));
const index_1 = require("./index");
const dotenv = __importStar(require("dotenv"));
dotenv.config();
// Indicate to index.ts to skip auto-running CLI mode
process.env.RUN_MODE = 'server';
const app = (0, express_1.default)();
const port = 3001;
app.use((0, cors_1.default)());
app.use(express_1.default.json());
// 1. Get all agent runs
app.get('/api/runs', async (req, res) => {
    try {
        const runsList = await database_1.db
            .select()
            .from(database_1.agentRuns)
            .orderBy((0, drizzle_orm_1.desc)(database_1.agentRuns.startedAt));
        res.json(runsList);
    }
    catch (err) {
        console.error('Error fetching runs:', err);
        res.status(500).json({ error: err.message });
    }
});
// 2. Get step logs for a run
app.get('/api/runs/:id/logs', async (req, res) => {
    try {
        const logsList = await database_1.db
            .select()
            .from(database_1.actionLogs)
            .where((0, drizzle_orm_1.eq)(database_1.actionLogs.runId, req.params.id))
            .orderBy(database_1.actionLogs.stepIndex);
        res.json(logsList);
    }
    catch (err) {
        console.error('Error fetching logs:', err);
        res.status(500).json({ error: err.message });
    }
});
// 3. Trigger a new run asynchronously
app.post('/api/run', async (req, res) => {
    const { targetUrl } = req.body;
    if (!targetUrl) {
        return res.status(400).json({ error: 'targetUrl parameter is required' });
    }
    const runId = crypto_1.default.randomUUID();
    // Run the agent loop completely async in the background
    const agent = new index_1.AutonomousAgent();
    agent.run({
        runId,
        targetUrl,
    }).then(() => {
        console.log(`[Server]: Run ${runId} finished successfully.`);
    }).catch((err) => {
        console.error(`[Server]: Run ${runId} encountered error:`, err);
    });
    // Return the new session ID immediately to permit polling
    res.json({ id: runId, status: 'PENDING', targetUrl });
});
app.listen(port, () => {
    console.log(`[API Server] Running on http://localhost:${port}`);
});
//# sourceMappingURL=server.js.map